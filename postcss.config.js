// PostCSS config for Tailwind CSS v4
// Tailwind v4 handles most PostCSS features internally
module.exports = {
  plugins: {
    // Tailwind v4 handles @import, autoprefixer, and nesting internally
    // No additional plugins needed
  },
};
